import React from 'react'
import { Paper } from 'material-ui'
import styled from 'styled-components'

const BlogWrapper = styled(Paper)`
    width: 40%;
    margin-right: 4%;
    margin-bottom: 3%;
    padding: 20px;
`

const RecentBlogPosts = (props) => (
    <BlogWrapper>
        
    </BlogWrapper>
);

export default RecentBlogPosts;